<Page x:Class="CustomOOBE.Views.NetworkSetupPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="Transparent" Loaded="Page_Loaded">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="700" Opacity="0" x:Name="ContentPanel">
            <TextBlock Style="{StaticResource TitleTextStyle}" Text="Configurar conexión a Internet" Margin="0,0,0,30"/>

            <Border Background="{DynamicResource SecondaryBackgroundBrush}" CornerRadius="12" Padding="20" Margin="0,0,0,20">
                <StackPanel>
                    <Grid Margin="0,0,0,15">
                        <TextBlock Style="{StaticResource SubtitleTextStyle}" Text="Redes disponibles" HorizontalAlignment="Left"/>
                        <Button Content="🔄 Actualizar" Click="RefreshButton_Click" Style="{StaticResource SecondaryButtonStyle}"
                                HorizontalAlignment="Right" Padding="15,5"/>
                    </Grid>

                    <ScrollViewer MaxHeight="400" VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="NetworksList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource InputBackgroundBrush}" CornerRadius="8"
                                            Margin="0,5" Padding="15" Cursor="Hand" MouseLeftButtonDown="NetworkItem_Click" Tag="{Binding}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="{Binding SSID}" FontSize="18" FontWeight="Medium" Foreground="{DynamicResource TextBrush}"/>
                                                <TextBlock Text="{Binding SecurityType}" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                            </StackPanel>
                                            <TextBlock Grid.Column="1" Text="📶" FontSize="24" VerticalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </Border>

            <TextBlock x:Name="StatusText" FontSize="14" Foreground="{DynamicResource AccentBrush}"
                       HorizontalAlignment="Center" Margin="0,0,0,20" Visibility="Collapsed"/>

            <Grid Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Style="{StaticResource SecondaryButtonStyle}" Content="Atrás"
                        Click="BackButton_Click" HorizontalAlignment="Left" MinWidth="120"/>
                <Button Grid.Column="1" Style="{StaticResource SecondaryButtonStyle}" Content="Saltar"
                        Click="SkipButton_Click" HorizontalAlignment="Center" MinWidth="120"/>
                <Button Grid.Column="2" x:Name="NextButton" Style="{StaticResource PrimaryButtonStyle}"
                        Content="Continuar" Click="NextButton_Click" HorizontalAlignment="Right" MinWidth="120" IsEnabled="False"/>
            </Grid>
        </StackPanel>

        <!-- Dialog para contraseña -->
        <Border x:Name="PasswordDialog" Background="#CC000000" Visibility="Collapsed">
            <Border Background="{DynamicResource BackgroundBrush}" CornerRadius="12" Padding="40" MaxWidth="500" MaxHeight="300" VerticalAlignment="Center" HorizontalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="Ingresar contraseña" Style="{StaticResource TitleTextStyle}" FontSize="24" Margin="0,0,0,20"/>
                    <TextBlock x:Name="NetworkNameText" FontSize="16" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,20"/>
                    <PasswordBox x:Name="PasswordBox" Style="{StaticResource ModernPasswordBoxStyle}" Margin="0,0,0,20"/>
                    <Grid>
                        <Button Content="Cancelar" Style="{StaticResource SecondaryButtonStyle}" Click="CancelPassword_Click" HorizontalAlignment="Left" MinWidth="120"/>
                        <Button Content="Conectar" Style="{StaticResource PrimaryButtonStyle}" Click="ConnectWithPassword_Click" HorizontalAlignment="Right" MinWidth="120"/>
                    </Grid>
                </StackPanel>
            </Border>
        </Border>

        <!-- Dialog de advertencia al saltar -->
        <Border x:Name="WarningDialog" Background="#CC000000" Visibility="Collapsed">
            <Border Background="{DynamicResource BackgroundBrush}" CornerRadius="12" Padding="40" MaxWidth="550" VerticalAlignment="Center" HorizontalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="⚠️ Continuar sin Internet" Style="{StaticResource TitleTextStyle}" FontSize="24" Margin="0,0,0,20" HorizontalAlignment="Center"/>
                    <TextBlock TextWrapping="Wrap" FontSize="16" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,15" TextAlignment="Center">
                        Si continúas sin conexión a Internet:
                    </TextBlock>
                    <StackPanel Margin="20,0,20,20">
                        <TextBlock Text="• No se podrán descargar programas" FontSize="14" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,5"/>
                        <TextBlock Text="• Algunas características estarán limitadas" FontSize="14" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,5"/>
                        <TextBlock Text="• No se verificarán actualizaciones" FontSize="14" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,5"/>
                    </StackPanel>
                    <TextBlock TextWrapping="Wrap" FontSize="15" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,25" TextAlignment="Center" FontWeight="Medium">
                        Es altamente recomendado conectarse a Internet
                    </TextBlock>
                    <Grid>
                        <Button Content="Volver" Style="{StaticResource PrimaryButtonStyle}" Click="CancelSkip_Click" HorizontalAlignment="Left" MinWidth="140"/>
                        <Button Content="Continuar sin Internet" Style="{StaticResource SecondaryButtonStyle}" Click="ContinueWithoutInternet_Click" HorizontalAlignment="Right" MinWidth="200"/>
                    </Grid>
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</Page>
