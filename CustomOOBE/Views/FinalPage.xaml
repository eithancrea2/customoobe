<Page x:Class="CustomOOBE.Views.FinalPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="#FF000000" Loaded="Page_Loaded">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Opacity="0" x:Name="ContentPanel" MaxWidth="1000">
            <TextBlock x:Name="Message1" Text="Muchas gracias por configurar tu equipo"
                       FontSize="48" FontWeight="SemiBold" TextAlignment="Center"
                       Foreground="White" Opacity="0" Margin="0,0,0,50" TextWrapping="Wrap"/>

            <TextBlock x:Name="Message2" Text="A continuación, disfruta"
                       FontSize="36" FontWeight="Normal" TextAlignment="Center"
                       Foreground="White" Opacity="0" Margin="0,0,0,80" TextWrapping="Wrap"/>

            <TextBlock x:Name="Message3" Text="Iniciando Windows..."
                       FontSize="24" FontWeight="Light" TextAlignment="Center"
                       Foreground="White" Opacity="0" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Page>
